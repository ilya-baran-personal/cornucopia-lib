# CmakeLists.txt in DemoUI

INCLUDE_DIRECTORIES(${CORNUCOPIA_SOURCE_DIR}/Cornucopia)

ADD_EXECUTABLE(DemoUI test)

TARGET_LINK_LIBRARIES(DemoUI Cornucopia)

INSTALL( TARGETS DemoUI RUNTIME DESTINATION bin )